<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meteor.chat.common.mapper.ContactMapper">

    <insert id="refreshActiveTime">
        insert into contact (uid, room_id, active_time, last_msg_id) values
        <foreach collection="uidList" item="uid" separator=",">
            (#{uid}, #{roomId}, #{sendTime}, #{msgId})
        </foreach>
        ON DUPLICATE KEY UPDATE
        active_time = if(#{msgId} > last_msg_id, #{sendTime}, active_time),
        last_msg_id = if(#{msgId} > last_msg_id, #{msgId}, last_msg_id)
    </insert>
</mapper>
